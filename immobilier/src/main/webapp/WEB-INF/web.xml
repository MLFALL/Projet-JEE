<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
    <display-name>Immo Rental</display-name>

    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Encoding filter -->
    <filter>
        <filter-name>EncodingFilter</filter-name>
        <filter-class>sn.isi.immobilier.filter.EncodingFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>EncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Auth filter: protège /app/* -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>sn.isi.immobilier.filter.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/app/*</url-pattern>
    </filter-mapping>

    <!-- Role-based protection can être gérée soit via un second filter (RoleFilter),
         soit à l'intérieur d'AuthFilter selon userRole en session. -->
    <filter>
        <filter-name>RoleFilter</filter-name>
        <filter-class>sn.isi.immobilier.filter.RoleFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>RoleFilter</filter-name>
        <url-pattern>/app/*</url-pattern>
    </filter-mapping>


    <!-- Resource reference (JNDI) pour DataSource -->
    <resource-ref>
        <description>PostgreSQL DataSource</description>
        <res-ref-name>jdbc/ImmoDS</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>



</web-app>